# P02-002 Contract Matrix

| Requirement                                  | Production contract                                                                                                                                                                                                     | Verification                                                                                                                                                                                          |
| -------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Closed input and classified match union      | `packages/domain/src/match/schemas.ts`: `MatchInputSchema`, `MatchClassificationSchema`, official/friendly 12-player team Schema, scrimmage source-12 / 6-vs-6 Schema, and exact Result-to-Input classification binding | Tests validate exact 12, five slots, no duplicates, invalid kind/scope rejection, deterministic 6-vs-6, and reject a different valid Result classification after result-ID recomputation              |
| Deterministic game/match/input identity      | `deriveGameId`, `deriveMatchId`, `deriveMatchInputHash` use Canonical V2 `idHash` only                                                                                                                                  | Match-input fixture rejects a swapped match identity even after its outer input hash is recomputed                                                                                                    |
| Anchor/event/fact/result/transcript identity | `MatchAnchorSchema`, `MatchEventSchema`, `MatchFactSchema`, `MatchTranscriptEntrySchema`, `MatchTranscriptSchema`, `MatchResultDraftSchema`, and `MatchProtocolBundleSchema`                                            | Three-Anchor fixture verifies adjacent cursor windows, previous-Anchor event coordinates, and dense per-segment local sequence; rehashed coordinate/local-sequence and skipped-Anchor variants reject |
| Event participant and team attribution       | `MatchProtocolBundleSchema` resolves event payload roles against the immutable HOME/AWAY registered-roster sets and the previous Anchor possession side                                                                 | A registered HOME shooter accepts; an unregistered shooter and an AWAY player attributed to HOME reject after event, fact, digest, and result hashes are recomputed                                   |
| Local command contract only                  | Independent closed `MatchCommandSchema`; no handler is exported                                                                                                                                                         | Transcript fixture binds a canonical command payload hash; unknown command kinds are outside the discriminated union                                                                                  |
| Canonical V2 / SHA-256                       | `packages/domain/src/core/canonical-v2.ts` sorts keys by JavaScript UTF-16 code units and rejects nonportable values                                                                                                    | `canonical-v2-golden.json` covers ASCII, Chinese, combining, supplementary-plane, and surrogate-pair ordering plus order-invariant hashes                                                             |
| Deterministic fixed point                    | `packages/domain/src/core/fixed-point.ts` uses safe integers and BigInt intermediary arithmetic                                                                                                                         | Bounds, clamp, signed half-up, multiplication, overflow rejection, invalid input, and fixed serialization tests                                                                                       |
| Sequential and keyed RNG                     | `packages/domain/src/core/rng-contract.ts` calls existing `nextRngState` exactly once; `packages/domain/src/match/keyed-rng.ts` derives 4×uint32 seed material and uint64 keyed draws                                   | Golden keyed draws, call-order/branch/cosmetic isolation checks, and exact four-call seed test                                                                                                        |
| Generic restricted effects                   | `packages/domain/src/match/effects.ts` and Schema whitelist base tactics/opponent policy only                                                                                                                           | Stable replacement, source merge ordering, additive/multiplicative caps, possession-only decrement, factual activation boundary, and Card rejection tests                                             |
| P01 root compatibility                       | Root `@sunny-court/domain` remains a thin Legacy P01 export; P02 uses explicit subpaths                                                                                                                                 | `tests/p02-002-package-exports.test.ts`, P02-001 subpath test, and frozen P01 regressions                                                                                                             |

The box-score fields are closed data shapes required by the MatchAnchor contract only. This slice
contains no box-score reducer, formula, aggregation, settlement, or real-match execution.

Actor tests cover the closed discriminated shape and the decision fields actually supplied to the
Schema. They do not claim that the contract detects a caller who fully reconstructs a different,
internally consistent actor/decision bundle and all derived hashes. This slice adds no signature or
trust-root mechanism.
