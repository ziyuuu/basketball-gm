# P00 Rollback

If workspace orchestration becomes a blocker, retain the same `apps/` and `packages/` dependency direction but run scripts from the root as a single repository. Do not collapse domain and platform adapters.
